<?php
function output_stubs($cache_directory, $functions) {
/*
  $classNames = array_unique(array_map(function($f){ return $f->className; }, $functions));
  print_r($classNames);

  foreach( $functions as $func ) {
    if ( $func->className == '__thiscall OmsDate' ) {
      print_r($func);
    }
  }
*/
}
